var nome = "Marlon" // string
var idade = 90 // number
var salvo = true // boolean
var cidade // undefined
typeof nome
'string'
typeof idade
'number'
typeof salvo
'boolean'
typeof cidade
'undefined'

var tipoNome = typeof nome
console.log(tipoNome)

var tipoIdade = typeof idade
console.log(tipoIdade)

var tipoSalvo = typeof salvo
console.log(tipoSalvo)

var tipoCidade = typeof cidade
console.log(tipoNome)

// Quando definimos uma variável sem dar valor a ela, esses dados são chamados de : Indefinido | Undefined