let nome = 'Marlon Braga Filho'

// A propriedade length de um objeto String contém o comprimento da string. length é uma propriedade read-only (somente leitura) de instâncias de string
let resultadoLength = nome.length

// O método indexOf() retorna o índice da primeira ocorrência do valor fornecido em searchValue, começando a busca a partir de fromIndex. Retorna -1 se o valor não for encontrado
let resultadoIndexOf= nome.indexOf('Braga')

console.log(resultadoIndexOf)


// Exercício 1
let nomeNovo = "Beltrano da Silva"
let resultado = nomeNovo.indexOf("da");
console.log(resultado);
